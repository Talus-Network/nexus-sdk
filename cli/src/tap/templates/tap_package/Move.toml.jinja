[package]
name = "{{ package_name }}"
version = "1.0.0"
edition = "2024.alpha"

# Keep the packages your TAP imports directly. MVR resolves their transitive
# dependencies and selects the publication for the active build environment.
[dependencies]
nexus_primitives = { r.mvr = "@talus/nexus-primitives" }
